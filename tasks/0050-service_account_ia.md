# Autenticação de service account pra IA

**Status:** pendente
**Prioridade:** P2
**Seção no ROADMAP:** 10. Infraestrutura / produção

Hoje o `ai-orchestrator` usa `service_role` direto (decisão aceita por ora, dado que hoje é um processo por empresa — ver tarefa `0024-topologia_orchestrator`), mas fica como pendência de endurecimento futuro.
