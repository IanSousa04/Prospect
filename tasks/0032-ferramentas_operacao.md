# Ferramentas de operação — confirmar cobertura completa

**Status:** pendente
**Prioridade:** P2
**Seção no ROADMAP:** 3. Camada de IA — MVP 2 e além

O catálogo de ferramentas previsto inclui, além das já em uso, `consultar_horario`, `consultar_taxa`, `consultar_regiao` e `consultar_politica` (domínio "Operação") e `buscar_conhecimento` (domínio "Conhecimento") — confirmar no `NOMES_FERRAMENTAS`/registry se todas têm executor real registrado (mesmo padrão de `criar_pedido` e as demais tools de escrita, que hoje só existem como nome no enum) e, para as que faltarem, tratar como as demais tools de leitura já existentes.
